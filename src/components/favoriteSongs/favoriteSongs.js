import { Redirect } from 'react-router';
import { Container } from './favoriteSongsStyle';

export default function FavoriteSongs(){
   return (
      <Container>
         <p>favorite Page</p>
      </Container>
   );
}